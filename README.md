# Fresh Adventure Go App

A fun Go web API with Gin!  

## Endpoints

- `GET /` → Welcome message  
- `GET /adventure/:name` → Returns a random adventure

## Commands

- Install dependencies:
  ```bash
  go mod tidy
  go mod download
